---
on:
  pull_request: &ignore-paths
    paths-ignore:
      - ".gitattributes"
      - ".gitignore"
      - "CHANGELOG.md"
      - "docs/**"
      - "README.md"

  push: *ignore-paths

name: build

permissions:
  contents: read

jobs:
  codeception:
    uses: yii2-framework/actions/.github/workflows/codeception.yml@670b6444a11798dc2b38f2563dbbcef063dfc0ab # v2.0.1
    with:
      codeception-command: vendor/bin/codecept run --env php-builtin --coverage-xml
      coverage-file: tests/support/output/coverage.xml
      extensions: dom, json, gd, imagick, intl, pdo_sqlite
      php-version: '["8.3","8.4","8.5"]'
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
